# Hostname or IP of this host used by UniFi devices for adoption — REQUIRED.
UOS_SYSTEM_IP=

# Optional: pin a fixed machine UUID. Leave empty to auto-generate once and persist.
UOS_UUID=

# UniFi OS Server version (informational for local builds).
UOS_SERVER_VERSION=5.1.21
